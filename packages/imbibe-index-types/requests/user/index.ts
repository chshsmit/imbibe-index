export interface RegisterBody {
  id: string;
  name: string;
  email: string;
  displayName: string;
}

export interface RegisterResponseData {
  id: string;
  name: string;
  email: string;
  displayName: string;
}